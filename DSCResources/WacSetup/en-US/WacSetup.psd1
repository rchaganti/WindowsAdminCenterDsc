ConvertFrom-StringData @'
    CheckThumbprint = Checking if {0} exists in local certificate store.
    InvalidCert = Certificate represented by {0} is not a valid certificate for the deployment.
    UsingCert = Using certificate thumbprint {0} for WAC install.
    NoCertFound = Certificate thumbprint {0} does not exist in local store.
    UsingSelfSigned = Using auto-generated self-signed certificate for WAC install.
    InstallWAC = Windows Admin Center will be installed.
    UninstallWAC = Windows Admin Center will be uninstalled.
'@
